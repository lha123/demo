package ${package};

/**
 * @author ${author}
 * @since ${date}
 */
public interface ${className}  {
#foreach($service in ${services})

    /**
    * ${service.title}
    * @param ${service.fromLowerCase}
    */
    ${service.vo} ${service.method}(${service.fromUpperCase} ${service.fromLowerCase});
#end
}
