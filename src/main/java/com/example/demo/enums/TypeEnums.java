package com.example.demo.enums;

public enum TypeEnums {
    no,
    eq,
    ne,
    like,
    likeLeft,
    likeRight,
    gt,
    ge,
    lt,
    le,
    in,
    notIn,
    is_null,
    is_not_null,
    apply, //date_format(dateColumn,'%Y-%m-%d') = {0}
    between,
    notBetween,
}
